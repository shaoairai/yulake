# ===== 後端開發環境 =====
FROM python:3.12-slim

WORKDIR /app

# 安裝依賴
COPY yulakeback/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 開發環境使用掛載，不需要複製程式碼
# 啟動 Flask 開發伺服器（支援熱重載）
CMD ["python", "app.py"]
